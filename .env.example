# Database
POSTGRES_DB=sensorhub
POSTGRES_USER=sensorhub
POSTGRES_PASSWORD=CHANGE_ME_DB_PASSWORD

# JWT (min 32 characters)
JWT_SECRET=CHANGE_ME_MIN_32_CHARACTERS_LONG

# Optional JWT settings (defaults shown)
# JWT_ISSUER=SensorHub
# JWT_AUDIENCE=SensorHub

# Environment
ASPNETCORE_ENVIRONMENT=Development

# Admin seeding (Development only)
SEED_ADMIN=false
SEED_ADMIN_EMAIL=admin@sensorhub.local
SEED_ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD
